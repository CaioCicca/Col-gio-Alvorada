import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 19,
    fontWeight: "bold",
    textAlign: 'center',
    color:'#274696',
    paddingBottom: 10
  },
});

export default styles;
